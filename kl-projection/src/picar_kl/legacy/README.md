# Legacy Imports

This package contains relevant copied assets from prior experiments.
These files are staged here so they can be refactored into the current `picar_kl` architecture.

Do not treat this namespace as the final runtime design.

